// 입면도(단열재 나누기도) 저장 포맷 타입 — 원천은 @ipg/shared (web·worker 공용).
// 페이지/유틸의 기존 import 경로("./types")를 유지하기 위한 재수출.
export type {
  ElevTypeDef,
  ElevBuildingDef,
  ElevUnitCounts,
  ElevTypeMatrix,
  ElevState,
  ElevSummary,
  DbElevProject,
  DbElevRevisionMeta,
  DbElevRevisionFull,
} from "@ipg/shared";
